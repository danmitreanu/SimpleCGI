info:
  name: Path serves static file with forwardPaths
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/statichtml/test
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
