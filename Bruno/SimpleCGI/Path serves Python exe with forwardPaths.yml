info:
  name: Path serves Python exe with forwardPaths
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/exe/follow/path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
